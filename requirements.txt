# Health Report Assistant — dependencies
# Fully deterministic pipeline: no LLM, no Vision API, no OCR required.

streamlit>=1.32.0
pdfplumber>=0.11.0
python-docx>=1.1.0

# Pillow is pulled in by pdfplumber; pinned here for reproducibility
Pillow>=10.0.0
